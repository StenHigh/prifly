## 1. Контракт и границы

- [x] 1.1 Контракт шага v11: поле границы (система, изменяющие операции, цель),
      обязательное при классе внешнего изменения и запрещённое без него.
- [x] 1.2 Компиляция: граница обязательна; retry-класс, допускающий слепой
      повтор, отказывается; прежние контракты байт в байт.
- [x] 1.3 Ворота старта и ассистируемого шага пропускают класс; программный шаг
      отказывается с причиной.

## 2. Разрешение доезжает

- [x] 2.1 Задание несёт разрешение и границу дословно; семейство схем 39
      описывает поле, прежние бандлы его не знают.
- [x] 2.2 Состояние 39 достижимо: Run, чей план объявляет границу, им
      запечатывается. Проверка достижимости — та же, что поймала 34 и 35.
- [x] 2.3 Класс не даёт прав на рабочую копию: сверка метки остаётся.

## 3. Политика

- [x] 3.1 Редакция политики 4.0.0 рядом с прежними тремя, неизменными.
- [x] 3.2 `allowed_effect_classes` читается при старте. Поле было объявлено с
      первой редакции и не проверялось нигде — список не ограничивал ничего.
      Вырезание проверки даёт красный.

## 4. Универсальность

- [x] 4.1 Ни одного продуктового имени в фикстурах и справочнике: система,
      операции и цель объявляются на стороне workflow, движок их не знает.
      Поймано владельцем на моих же фикстурах.

## 5. Ворота

- [x] 5.1 `make ci-check` зелёный: fmt-check 324 файла, refusal-check 171.
- [ ] 5.2 Проверка сценария настоящим Run — за проектом после обновления.
