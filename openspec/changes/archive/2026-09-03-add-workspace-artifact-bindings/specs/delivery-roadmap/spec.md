## ADDED Requirements

### Requirement: Canonical AI Factory plan bridge остаётся active delivery work

Единый current backlog MUST хранить `add-workspace-artifact-bindings` как
active high-priority change до завершения его OpenSpec tasks. Запись MUST
называть prerequisite: declared Workspace tree binding с sealed manifest и
compatible assisted-host protocol; следующий шаг: применить change и выполнить
targeted Fast/Full/Ultra, provenance и drift checks. Она MUST отделять этот
scope от живого `aif-classic` pilot, upstream skill-package compatibility и
pilot qualification.

#### Scenario: Команда выбирает следующую AIF работу
- **WHEN** reader открывает delivery roadmap до завершения change
- **THEN** он видит plan bridge как активную работу с её prerequisite и не
  принимает tree contract за qualification живого pilot
