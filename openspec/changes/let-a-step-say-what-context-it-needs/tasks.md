## 1. Объявление

- [ ] 1.1 `internal/flow`: шаг объявляет требование к контексту хоста (изоляция или непрерывность) в новой версии авторского контракта; умолчание сохраняет прежнее поведение. Проверка: тест — пакет без объявления компилируется и ведёт себя как прежде; с объявлением поле доезжает до плана.
- [ ] 1.2 Задача, выдаваемая хосту, несёт объявление. Проверка: тест — `session task` шага с изоляцией называет её; шаг без объявления документ не меняет.

## 2. Хост

- [ ] 2.1 Раннер называет обе формы и что делать, если хост не умеет требуемую: сказать честно в отчёте. Проверка: тест раннера на обе фразы; пины обновлены.

## 3. Документы и ворота

- [ ] 3.1 `examples/troubleshooting.md` и authoring reference называют поле и его цену: свежий контекст полезен проверяющему шагу и вреден пишущему, потому что тот не знает ловушек, которые предыдущий шаг уже понял. Проверка: `python3 -B test/e2e/test_examples.py`.
- [ ] 3.2 `make ci-check`, `make e2e`, `make race` зелёные; счётчики записаны в change.
