# aif-warmup: project context

- Also list the active OpenSpec changes: every `openspec/changes/*/tasks.md`
  outside `archive/`, with its ticked/unticked counts. Report them under
  **Direction** as the work in flight; they are the task lists, the roadmap
  spec is the boundary.
- `CONTEXT_STATE.md` is the sessions' log (200 KB): name it as orientation,
  do not load it.
