# Надёжное продолжение из незамерженной реализации

Ветка: `prifly/fbc0e9b863911544e0640c63ea287012c938fd2501b67814b3fceaf81496b175`  
Дата: 2026-09-23

## Original Request

да давай доделаем что начали и потом сделаем все что сейчас обсудили. Что бы в дальнейшем не допускать таких проблем.

## Settings

- Тесты: включены; логирование: minimal; документация: checkpoint не требуется.
- Профиль: full; standing gate: `make ci-check`, `make e2e`, `openspec validate --all --strict`, `git diff --check`.
- Изменения кода и плана выполнять только в claim worktree. Публикация релиза — отдельное действие владельца после проверок.

## Roadmap Linkage

Milestone: "none"  
Rationale: пользователь не выбрал milestone; действующий delivery roadmap уже относит `continue-existing-implementation` к очереди продолжения и recovery.

## Requirements Reconciliation

- Источник поведения: `openspec/changes/continue-existing-implementation/specs/cli-protocol/spec.md` и `design.md`; `tasks.md` отмечает 1.4 и 3.3 открытыми. `AGENTS.md`, `openspec/SOURCE-OF-TRUTH.md` и `.ai-factory/RULES.md` определяют процесс и совместимость.
- Чистый основной checkout + полный SHA незамерженной реализации + `worktree`: `--prepare` включает SHA в digest, start создаёт новый claim и Run от SHA. Исходный Run и worktree остаются прежними. Проверка: CLI fixture с реальным terminal partial source.
- HEAD уже содержит реализацию без флага: прежний путь остаётся допустимым. Сокращённый/неизвестный SHA, `checkout` с флагом, неподходящий source: отказ до claim/Run. Проверка: та же CLI fixture.
- Уже активный continuation от того же source: `--prepare` отказывает с его Run ID и status из read-only `Engine.Runs`; обычный start повторно проверяет и отказывает с тем же ID. Намеренное независимое продолжение требует явного `--allow-duplicate-continuation`, входящего в digest. Простое чтение перед start не доказывает атомарную защиту от двух одновременных запусков; не обещать её без отдельного authority admission contract.
- `project continue` обязан оставаться универсальным: проверка чистоты и актуальности ветки принадлежит проектному preflight. Обновление runner не должно автоматически переписывать `PROJECT.md`.

## Tasks

### Фаза 1 — CLI и доказательство пути

- [x] 1.4a — Добавить в `cmd/prifly/project_continuation_test.go` сквозной fixture terminal partial source: основной checkout чистый, реализация только в Git worktree, `--prepare` и start с одинаковым SHA, новый claim и Run указывают на SHA, старые worktree и source Run не меняются. Включить отказы и stale digest; базовый адресный тест уже запущен успешно. Логирование: выводить точные отказ/Run ID лишь в сообщении теста.
- [x] 1.4b — В `cmd/prifly/project_continuation.go` сделать `project_continue_unrelated_head` практической подсказкой на `project continue ... --implementation-head <full SHA> --prepare`; дополнить CLI тестом кода и текста. Не вычислять SHA за владельца. Логирование: только существующий Problem envelope.
- [x] 1.4c — В `cmd/prifly/project_start.go` и его summary использовать `Engine.Runs` для активного потомка source Run: `--prepare` отказывает с ID/status, start повторно проверяет; `--allow-duplicate-continuation` разрешает сознательную альтернативу и участвует в review digest. Добавить тест с существующим дочерним Run и сохранить допустимость параллельных Runs разных source. Не выдавать проверку за атомарную защиту от одновременного старта — это отдельный authority contract. Логирование: Run ID в диагностике, без чтения внутренних артефактов.

### Фаза 2 — инструкция runner и спецификация

- [x] 3.1 (расширение) — В `cmd/prifly/project.go` описать основной checkout + полный `--implementation-head` в prepare/start и проверку уже активного continuation. Сохранить точный прежний runner как frozen variant, обновить pins и оба генерируемых `SKILL.md`; `PROJECT.md` не заменять. Проверка: `TestProjectRunnerUpdateReplacesEveryReleasedRunner` и pin tests. Логирование: не требуется, runner — текстовый артефакт.
- [x] 1.4 (сверка) — В активном OpenSpec change уточнить новые сценарии и отметки задач; не объявлять 3.3 закрытым без terminal quality outcome в SMSPlace. Проверка: strict OpenSpec validation. Логирование: не требуется.

## Commit Plan

- После CLI теста/диагностики: `fix(project): make continuation source explicit and reviewable`.
- После runner/spec согласования: `docs(project): guide unmerged continuation safely`.

## Проверенные команды плана

- `/Users/sh/PhpstormProjects/Pri-Fly/github/.tools/go/bin/go test ./cmd/prifly -run 'TestContinuationSelectsUnmergedImplementationCommit|TestProjectRunnerUpdateReplacesEveryReleasedRunner' -count=1` — 2026-09-23, exit 0 на этой claim-ветке.
- `git diff --check` — 2026-09-23, exit 0 на этой claim-ветке до правок.

Пункт 3.3 требует отдельного реального Run в SMSPlace после выпуска подходящего бинарника. Этот Run не утверждает, что такой исход уже получен.

## Прогресс реализации

- `make ci-check`, `make e2e`, `openspec validate continue-existing-implementation --strict`, `git diff --check` — pass на claim-ветке 2026-09-23.
- Read-only пилот: для source `run:5c6fd74e…` с активным child `run:b04c1fc…` обычный prepare вернул `project_continue_active_child` с ID/status; prepare с явным `--allow-duplicate-continuation` вернул digest `sha256:2fc1e64c…` и выбранный HEAD `542493c…`. Ни новый claim, ни Run не создавались.
- `TestCLIContinuationFromUnmergedImplementation` прошёл: переносимый terminal partial source, exact unmerged SHA, новый claim и первый read-only gate, отказы dirty checkout/invalid SHA/stale digest/active child. Открыт только реальный terminal quality outcome SMSPlace (3.3).
