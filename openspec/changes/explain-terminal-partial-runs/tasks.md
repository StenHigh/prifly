## 1. Терминальная сводка

- [ ] 1.1 Разделить живой и терминальный empty-state в обзоре Run, отдельно объяснить `completed` + `partial`; проверить UI-тестом, что терминальный Run не показывает сообщение «Активная работа и ожидания не записаны» или ожидание агента.
- [ ] 1.2 Показывать только доказанную фактическим маршрутом причину `partial` и навигацию к принятому результату/артефакту; проверить UI-тестами однозначный `needs_revision` и неоднозначный либо отсутствующий источник причины.

## 2. Проверка границ

- [ ] 2.1 Проверить UI-тестами, что внешняя работа в чате не делает исходный Run активным, проектная рекомендация не становится командой Pri-Fly, а другие терминальные исходы не получают объяснение для `partial`; выполнить `node cmd/prifly/monitor_ui_test.cjs`.
- [ ] 2.2 Проверить связанный исторический сценарий read-only просмотром, выполнить `openspec validate explain-terminal-partial-runs --strict` и `git diff --check`, затем проверить по `git diff --name-only`/`git status --short`, что historical evidence, сохранённые Runs и публичные JSON-контракты не затронуты.
