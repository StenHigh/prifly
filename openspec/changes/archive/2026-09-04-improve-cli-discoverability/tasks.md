## 1. Читаемость вызова

- [x] 1.1 Разбирать запрос справки и версии до открытия authority: `--help`/`-h` у любой подкоманды печатает её строку использования, `--version` работает как глобальный флаг, `help TOPIC` фильтрует ту же таблицу, из которой собрана общая справка. Тесты: справка каждой подкоманды не открывает authority и даёт exit 0, `help session` короче полной справки и содержит только её строки, неизвестная тема даёт `invalid_usage` (`go test ./cmd/prifly -run 'Help|Usage'`). (сделано: `--help`/`-h` и `--version` разбираются в глобальных флагах до открытия authority, `helpTopic` фильтрует ту же таблицу, из которой собрана общая справка, включая альтернативы вида `status|next|explain`; `TestHelpAndVersionAnswerWithoutAnAuthority` зелёный)
- [x] 1.2 `prifly schema` без имени печатает доступные имена контрактов; declared reference (`core:schema/step-result`) принимается наравне с `StepResult` через таблицу из builtin definitions. Тесты: список непуст и содержит `StepResult` и `SessionSubmissionV5`, обе формы имени дают одинаковые байты, неизвестное имя по-прежнему `unsupported_contract` (`go test ./cmd/prifly ./internal/runtime -run Schema`). (сделано: `PublicSchemaNames` и `flow.ProtocolSchemaNames` дают 354 имени, `contractNameForReference` переводит `core:schema/step-result` в `StepResult`; тест проверяет, что каждое перечисленное имя резолвится и обе формы дают одинаковые байты)
- [x] 1.3 Добавить `project local set --executable PATH`: переписывает только `prifly_executable`, сохраняет `authority_root`, отказывает при отсутствии `.prifly/local.yaml`. Тесты: путь заменён, остальные поля байт-в-байт прежние, отсутствие файла отказано именованно (`go test ./cmd/prifly -run ProjectLocal`). (сделано: заменяется только строка `prifly_executable`, остальные строки байт-в-байт прежние, относительный путь и отсутствие файла отказаны именованно)

## 2. Отказы называют предмет

- [x] 2.1 Отделить `authority_not_found` от `not_found` при открытии authority по `--project`; полученный путь уходит в `violations`, safe next action называет `--project`. Тесты: несуществующий каталог и каталог без authority дают `authority_not_found`, отсутствующий Run внутри существующей authority по-прежнему `not_found` (`go test ./cmd/prifly ./internal/runtime -run 'Authority|NotFound'`). (сделано: `Open` различает несуществующий путь и каталог без `prifly.json`, оба дают `authority_not_found` с путём в сообщении и safe next actions `init`/`doctor`; отсутствующий Run внутри рабочей authority остаётся `not_found`)
- [x] 2.2 `SessionTask` при живом Run без ожидающего host attempt отвечает `no_active_handoff` с `run.explain` и `run.drive` в safe next actions; отсутствующий Run сохраняет прежний код. Тесты: оба случая различимы по коду, Run не меняется (`go test ./internal/runtime -run SessionTask`). (сделано: `no_active_handoff` с `run.explain`/`run.drive`; два прежних теста, ожидавших `ErrNotFound` для отсутствующей передачи, переведены на новый код через `refusalCode`)
- [x] 2.3 `invalid_usage` глобальных аргументов повторяет полученное значение. Тест: обрезанный путь виден в `violations` (`go test ./cmd/prifly -run Usage`). (сделано: `--project` и `--format` называют полученное значение; `TestUsageRefusalRepeatsTheReceivedValue`)
- [x] 2.4 Отказ при запечатывании шага с недопустимым `effects.class` называет обе границы — квалификацию профиля и контракт assisted-шага. Тест: сообщение содержит обе, шаг не запечатан (`go test ./internal/runtime -run 'Effect|Unsupported'`). (сделано: сообщение называет и неквалифицированный профилем класс, и сужение assisted-контракта до `workspace_write`/`none`; проверено через `validateAssistedStep` на настоящем плане)

## 3. Самоописание handoff и видов

- [x] 3.1 Записи закреплённого контекста писать под именем, производным от ref id, и заполнять `LocalPort.Ref`; порядок перестаёт быть носителем смысла. Тесты: два закреплённых контекста различимы по имени файла и по манифесту, байты не изменились (`go test ./internal/runtime -run 'Context|Session'`). (сделано: файл называется по ref id и версии через `skillFileName`, дубликат имени даёт integrity-отказ; `TestPinnedContextFilesAreNamedAfterTheirReference` сверяет имя и digest каждой записи)
- [x] 3.2 `run status` называет счётчики по тому, что считают, и печатает число запечатанных выходов шагов отдельно от выходов Run. Тест: после захвата дерева счётчик шагов не ноль (`go test ./internal/runtime ./cmd/prifly -run 'Status|Render'`). (сделано: строка печатает `run_outputs` и `step_outputs`, второй считает выходы принятых результатов attempt-ов)
- [x] 3.3 `run next` для Run с ожидающим host attempt добавляет `session.task` в `safe_next_actions`, `action` остаётся `idle`; `explain` называет ожидание словами. Тесты: safe next actions содержат чтение задания, опубликованный enum не менялся (`go test ./internal/runtime -run 'Next|Explain'`). (сделано: `session.task` добавляется в `safe_next_actions`, `action` остался `idle`, опубликованный enum не менялся)
- [x] 3.4 `prifly update` печатает адрес, по которому проверялся release. Тест: ответ содержит base URL, поведение обновления не изменилось (`go test ./internal/release ./cmd/prifly -run Update`). (сделано: поле `source` в `prifly-update/1` несёт release base URL при обоих исходах)

## 4. Описания в схемах

- [x] 4.1 Добавить `description` для `WorkspaceTreeLocation.path`, `SessionSubmission.result` и `schema_version` результата шага; перегенерировать bundles и обновить immutability-тесты. Проверить `make schema-check` и что validation не изменилась: прежде валидные документы валидны, прежде невалидные — нет (`go test ./internal/runtime -run Contract`) (сделано: три описания добавлены через `generator.describe`, bundles перегенерированы; diff по `internal/runtime/*.schema.json` и `schemas/` — 50 добавленных строк `description` и ноль изменений формы, `required`, `enum`, `const` или `additionalProperties`; отпечатки `sessions` и `waivers` в `TestContextPublicSchemasPreservePublishedBundles` пересняты с записью причины — владелец единственный потребитель и снял правило неизменности для аннотации).

## 4a. Достижимость авторских форм

- [x] 4a.1 Отдавать authoring schemas (`schemas/authoring/*.schema.json`) по имени документа той же командой, что и контракты обмена, и перечислять их в индексе имён. Тесты: индекс содержит `extension` и `workflow-catalog`, каждое перечисленное имя резолвится, байты совпадают с файлом на диске (`go test ./cmd/prifly -run Schema`). (сделано: девять файлов встроены в `internal/runtime/authoring/`, отдаются по имени документа из editor-манифеста — `extension-v1`, `workflow-v1` и другие, всего 8 документов; индекс вырос с 354 до 362 имён; тест сверяет каждый встроенный документ с распространяемым файлом байт-в-байт)
- [x] 4a.2 Назвать в справке каталог authoring-примеров и указать, что полный непустой пример `extensions` лежит в `extension-authoring-reference.yaml`. Тест: справка содержит путь каталога (`go test ./cmd/prifly -run Help`). (сделано: строка `schema` в справке называет `examples/authoring/` и `extension-authoring-reference.yaml` как полный пример `extend.yaml` с непустым `extensions`)
- [x] 4a.3 Отказ `validate`/`preview` для пути вне authority называет `project compile --output` как проверку авторской папки без создания Run. Тест: отказ содержит имя команды, поведение внутри authority не изменилось (`go test ./cmd/prifly -run 'Validate|Preview'`). (сделано: отказ по пути вне authority называет `project compile --repository … --output …` как проверку авторской папки без создания Run; поведение внутри authority не изменилось)

## 4b. Расширения читаются без перебора

- [x] 4b.1 Отказы `project_extension_unknown_workflow` и `project_extension_unknown_step` перечисляют известные имена в обеих проверках (`project extend` и `project compile`); `project_extension_requires_full_yaml` сообщает, что шаг с входами пишется собственным графом, а не более полной формой записи. Тест: тексты содержат список известных имён и новую формулировку (`go test ./cmd/prifly -run Extension`). (сделано: оба места печатают `(known: …)` — имя workflow и отсортированный список шагов; сообщение о входах переписано на «шаг с входами пишется собственным графом»)
- [x] 4b.2 Справка `project start` называет `--preflight-answer`, `--package-profile`, `--decision-policy` и `--expected-decision-catalog-digest` и предупреждает, что профиль выбирается один раз. Тест: справка содержит все четыре флага (`go test ./cmd/prifly -run HelpNamesTheQuestionnaire`). (сделано: четыре флага показаны, плюс предупреждение, что профиль выбирается один раз и решение с назначением package_profile тогда не отвечают)
- [x] 4b.3 Объяснить в `extension-authoring-reference.yaml` короткую форму имён, замысел `between` (замещается ребро, поэтому хвост графа не отрывается) и запрет входов у вставляемого шага. Проверить `make examples`. (сделано: комментарий объясняет короткие имена против `id` внутри файла, замещение ребра вместо точки и запрет входов; `make examples` зелёный)

## 4c. Запуск называет package

- [x] 4c.1 `projectInstalledWorkflowPath` отказывает именованно: называет id и версию запечатанного package и различает «не установлен», «не доверен» и «другие bytes»; советует `package list`. Тест: отказ несёт имя и причину, голого `local.ErrNotFound` в этом месте не осталось (`go test ./cmd/prifly -run SealedPackageLookup`). (сделано: сообщение называет `id@version` и одну из трёх причин, тот же проход собирает запись с совпадающими id и версией; голый `local.ErrNotFound` из этого места убран)
- [x] 4c.2 Закрепить тестом уже существующее именование отсутствующей папки объявленного package, потому что в одном прогоне оба отказа читались как один безымянный `not_found` (`go test ./cmd/prifly -run MissingPackageFolder`). (сделано: `project_profile_invalid: package source does not exist` закреплён на объявленном пакете без папки)

- [x] 4c.3 Стабильный код доходит до диагностики драйвера так же, как до отказа команды: `driverFailureCode` распознаёт `*flow.Problem` и голый код помимо `*local.Rejection`. Тесты: pre-handoff drift даёт `workspace_tree_input_drift` вместо общего кода, существующий файл выхода — `workspace_tree_output_exists` и остаётся нетронутым (`go test ./internal/runtime -run WorkspaceTree`).

- [x] 4c.4 Read view несёт `Accepted.Summary` и `Report.Summary`/`Limitations`; credentials, envelope, candidate и raw definitions по-прежнему вычищаются. Тесты: сообщённый summary доходит до вида целиком, конверт и хэши токенов в нём отсутствуют, telemetry прежняя (`go test ./internal/runtime -run 'AcceptedSummaryReaches|Observability'`).

## 5. Гейты

- [x] 5.1 `make ci-check`, `make e2e`, `make race` (или GitHub `race.yml`), `openspec validate improve-cli-discoverability --strict`, `git diff --check`; убедиться, что `openspec/changes/archive/` не изменился и что изменения `internal/runtime/*.schema.json` и `schemas/` ограничены добавленными `description` (`git diff` по этим путям не содержит правок `required`, `enum`, `const` и `additionalProperties`) (сделано на финальном коде: `make ci-check` и `make e2e` зелёные, полный локальный `make race` зелёный без гонок — `internal/runtime` 992 s, `cmd/prifly` 87 s; `openspec validate --strict` проходит; `git diff --check` чист; `openspec/changes/archive/` не изменён; изменения в `internal/runtime/*.schema.json` и `schemas/` — только добавленные `description`, плюс новый встроенный каталог `internal/runtime/authoring/` как точная копия `schemas/authoring/`, что проверяется тестом) (сделано дважды: после блока 4a и повторно после блока 4b; в обоих прогонах `ci-check`, `e2e`, `make examples` и полный локальный `race` зелёные — последний прогон `internal/runtime` без гонок; `openspec validate --strict` проходит, `git diff --check` чист, архив не изменён, правки схем ограничены добавленными `description` и точной копией `schemas/authoring/` в `internal/runtime/authoring/`) (сделано трижды: после блоков 4a, 4b и 4c; в последнем прогоне `ci-check`, `e2e`, `examples` и полный локальный `race` зелёные, гонок нет; `openspec validate --strict` проходит, `git diff --check` чист, архив не изменён, правки схем ограничены добавленными `description` и точной копией `schemas/authoring/`) (сделано на финальном коде: `ci-check`, `e2e`, `examples` и полный локальный `race` зелёные — `internal/runtime` 999 s без гонок; `openspec validate --strict` проходит, `git diff --check` чист, `openspec/changes/archive/` не изменён; правки схем ограничены добавленными `description` и точной копией `schemas/authoring/` в `internal/runtime/authoring/`, отпечатки двух bundles пересняты с записью причины в тесте).
- [x] 5.2 Пройти на dev-сборке список замеров из отчётов pilot: справка подкоманд, `--version`, `help session`, `schema` без имени, неверный `--project`, `session task` без передачи, `run status` после захвата; записать exact ответы в этот файл (сделано на dev-сборке, часть — на живом прогоне `aif-classic`):
  - `session task --help` и `run status --help` печатают строку использования своей команды, authority не открывается;
  - `--version` отдаёт версию; `help session` — 9 строк против 117 полной справки;
  - `schema` без имени печатает 354 имени контрактов; `core:schema/step-result` и `StepResult` дают одинаковые байты;
  - `--project /no/such/dir` → `authority_not_found`, `safe_next_actions: ["init","doctor"]`; отсутствующий Run внутри рабочей authority остаётся `not_found`;
  - `run status` на прогоне с захваченным планом печатает `run_outputs=0 step_outputs=2` вместо прежнего `outputs=0`;
  - `run next`, пока задание держит host, называет `session.task` среди безопасных действий (`action` остался `idle` по решению владельца);
  - каталог контекста содержит `aif-context-aif-implement-1.0.0` и `aif-context-aif-implement-bridge-1.0.0` вместо `00`/`01`;
  - `no_active_handoff` для живого Run без передачи покрыт `TestSessionTaskSeparatesAnAbsentHandoffFromAnAbsentRun`: через CLI такое состояние требует завершённого прогона.
