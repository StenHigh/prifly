## 1. Там, где класс выбирается

- [x] 1.1 `examples/authoring/step-authoring-reference.yaml`: блок `effects`
      называет измеряемое и объявляемое, без обещания того, чего движок не
      может.

## 2. Там, где за ответом приходят

- [x] 2.1 `examples/troubleshooting.md`: раздел про шаг, которому нужно
      опубликовать ветку или создать MR, с двумя честными путями и прямым
      указанием, что вставить `git push` в worker с `class: none` — обход, а не
      решение.

## 3. Ворота

- [x] 3.1 `make ci-check` зелёный: fmt-check 317 файлов, refusal-check 170 файлов,
      бандлы схем без изменений.
