## 1. Совместимость прежде функции

- [x] 1.1 Тест на запечатанном графе ревизии 4 и 5. Написан первым; при
      расширении списка без гейта краснеет обеими ревизиями с `missing_handler`
      на `blocked` — то есть ловит именно «Run, читавшийся вчера, не читается».
- [x] 1.2 `verdictsRequiredBy(workflowVersion)`; `checkVerdictCoverage` берёт
      набор оттуда, а не из глобального `StepVerdicts`.

## 2. Вердикт

- [x] 2.1 `blocked` в `StepVerdicts`: законен всегда.
- [x] 2.2 Новая ревизия WorkflowRevision требует для него маршрут либо
      объявленную невозможность; граф без этого отказывается на компиляции.
- [x] 2.3 Запечатанный граф 4/5 без маршрута: принятый результат сохранён.
      Приём результата расширен до `StepResultV2`, сужает контракт самого шага
      (`driver.go:1671`, `sessions.go:921`). Первая версия теста звала валидатор
      напрямую и оставалась зелёной при возврате сужения — переписан через
      `observeResult`, теперь краснеет.
- [x] 2.4 `RoutedVerdicts` нёс хосту литеральный список из четырёх вердиктов —
      третью копию набора. Теперь читает `flow.StepVerdicts`. Текст раннера не
      менялся, пин-тест зелёный.

## 3. Читателю и автору

- [x] 3.1 `examples/authoring/step-authoring-reference.yaml` и справочник
      ревизий: когда объявлять маршрут для `blocked`.
- [x] 3.2 `examples/troubleshooting.md`: gate вернул `blocked` — что это значит
      и чего не значит.

## 4. Ворота

- [x] 4.1 `make ci-check` зелёный: fmt-check 319 файлов, refusal-check 170;
      `make e2e` зелёный. Схемы: v3, v4, v5 байт в байт прежние, v6 новый.
- [x] 4.2 Сообщить пакету: без возврата `blocked` из gate и маршрута в графе
      наблюдаемое изменение — только отсутствие ложного цикла исправления.

## 5. Найдено по ходу

- [x] 5.1 Четыре копии набора вердиктов/ревизий в коде: enum схемы ревизии 4
      строился из растущего `StepVerdicts` (менял опубликованные V4 и V5),
      `routedVerdicts` и `retries_test` перечисляли четыре литералом, список
      допустимых ревизий в `invocation.go` — пятым местом. Сведены к
      `VerdictsRequiredBy` и `WorkflowRevisions`.
- [x] 5.2 Артефакт принятого результата помечался контрактом v1 всегда. Теперь
      несёт объявленный шагом `result_schema_ref`: пометить результат с новым
      вердиктом контрактом, который его не называет, — та же неправда.
- [x] 5.3 0.13.49 вышел, умея компилировать ревизию 6 и объявляя, что не умеет:
      `workflow_definition_versions` показывал `["1"…"5"]`. Именно этот документ
      пакет читает перед внедрением, то есть сборка говорила читателю обратное
      тому, что делает. Проверка читает обе стороны — что принимает компилятор и
      что заявляет документ, — потому что каждая по отдельности и уехала.
