## 1. Данные происхождения и группировка

- [x] 1.1 Добавить в локальную read-only проекцию Run сохранённые `fork.source_run_id` и `fork.reason` без изменений state/schema; проверить `go test ./internal/runtime -run Monitor` на старом Run без fork и на продолжении/восстановлении.
- [x] 1.2 Построить в каталоге группы по `(source, run_id)` с parent-first порядком, явным отсутствующим родителем и независимыми корнями при одинаковом названии; проверить `go test ./cmd/prifly -run Monitor` на цепочке #154 с несколькими ветвями и тремя уровнями.
- [x] 1.3 Добавить ограниченную пагинацию групп и дочерних строк с сохранением плоского режима; проверить каталог тестом на истории более 200 Run и ветви длиннее одной страницы без пропусков/дубликатов.

## 2. Интерфейс списка

- [x] 2.1 Отобразить в первой колонке отступ, соединители, текст вида связи и доступную кнопку раскрытия; проверить `node cmd/prifly/monitor_ui_test.cjs` для 1–3 уровней, отдельного status/outcome каждой строки и отсутствующего родителя.
- [x] 2.2 Добавить переключение иерархический/плоский вид, сохранить раскрытие, выбор и позицию при опросе/возврате, а поиск и фильтры показывать с путём предков как контекстом; проверить UI-тестами клавиатуру, узкий экран, прямую ссылку и поиск производного Run.
- [x] 2.3 Показать рядом с собственным статусом строки последний по времени доступный производный Run и прямую ссылку на него даже при свёрнутой ветви; проверить на #154 и тестом фильтр, равное время и разницу статусов родителя/потомка.
- [x] 2.4 Поставить корни и sibling Run в иерархии по времени создания от ранних к поздним; проверить ветвящуюся цепочку, равное время и пагинацию без дублей.
- [x] 2.5 Добавить в «Вид» вариант «Связанные Run · ранние снизу» для обратного порядка корней и соседних Run с сохранением parent-first, фильтров и порционной загрузки; проверить оба порядка на ветвях, равном времени и страницах.

## 3. Сквозная проверка

- [x] 3.1 Проверить read-only на сохранившейся цепочке #154, что `recovery` не удваивает родителя, а независимые Run с тем же заголовком не вложены; выполнить `go test ./internal/runtime ./cmd/prifly`, `node cmd/prifly/monitor_ui_test.cjs`, `openspec validate group-related-runs-in-monitor --strict`, `git diff --check` и проверить по `git status --short`, что protected historical evidence и опубликованные схемы не менялись.
- [x] 3.2 Повторить регрессионную проверку, пересобрать и обновить локальный монитор, убедиться через `/api/runs` на #154, что свёрнутая ошибка родителя не скрывает успешное последнее восстановление.
