## 1. Читательские статусы

- [x] 1.1 Заменить неоднозначные русские подписи `ready`, `pending` и `running` в мониторе, сохранив сохранённые коды; проверить точные подписи unit-проверкой `node cmd/prifly/monitor_ui_test.cjs`.

## 2. Читаемость схемы

- [x] 2.1 Разнести порты соединений общего узла и семантически выделить фактический путь выбранного WorkflowInvocation; проверить развилку и отсутствие совпадающих портов в `node cmd/prifly/monitor_ui_test.cjs`.
- [x] 2.2 Добавить стили выделенного и приглушённого пути для обеих тем; проверить e2e монитора командой `python3 test/e2e/verify-monitor.py`.
- [x] 2.3 Заменить равноправную BFS-сцену на layout с центральным фактическим путём и отдельными каналами для достижимых, return и terminal routes; проверить отсутствие пересечений на фиксированном плотном fixture в `node cmd/prifly/monitor_ui_test.cjs`.
- [x] 2.4 Классифицировать каждый узел как выполненный, текущий, достижимый, исключённый с записанной причиной либо неизвестный; проверить все классы и отсутствие выдуманной причины в `node cmd/prifly/monitor_ui_test.cjs`.
- [x] 2.5 Заменить сырой inspector карточкой узла с закреплённым описанием, входящими/исходящими переходами, доступными данными и записанными причинами; проверить rendered markup в `node cmd/prifly/monitor_ui_test.cjs`.

## 3. Приёмка

- [x] 3.1 Проверить OpenSpec и ограниченный diff командой `openspec validate clarify-monitor-run-status-graph --strict && git diff --check`; подтвердить, что historical evidence и сохранённые JSON-поля не изменялись.
