# aif-archive: project context

Do not run in this repository. It keeps one history: a Run's plan under
`.ai-factory/plans/` is deleted in the commit that archives its OpenSpec
change (git keeps the bytes), and the roadmap is the OpenSpec spec
`openspec/specs/delivery-roadmap/spec.md` with its own archive. Say so and stop.
