# Задачи

## 1. Сужение

- [x] 1.1 Провести до компилятора признак «эта версия достигнута вставкой»: он
  уже вычисляется в `project_build.go:152` как `raisedByInsertion` и доходит до
  `projectValidatePackageWorkflows`, где сегодня влияет только на текст отказа.
  Проверка: признак не появляется в опубликованных схемах и не меняет байты
  `workflow-revision-v3` и `workflow-revision-v4`.
- [x] 1.2 При включённом признаке применять `checkVerdictCoverage` только к
  стадиям, несущим `impossible_verdicts`. Проверка вырезанием: со снятым
  сужением расширенный пакет прежней ревизии снова отказывает `missing_handler`
  на стадии пакета.
- [x] 1.3 Обратная проверка вырезанием: документ, написанный под этой ревизией
  автором и оставивший вердикт без маршрута, обязан отказывать по-прежнему —
  сужение не должно перелиться на авторский случай.

## 2. Предупреждение вместо молчания

- [x] 2.1 При сужении называть узлы пакета с незакрытыми вердиктами и того, кто
  может их закрыть. Проверка: предупреждение содержит идентификаторы узлов, а не
  их число, и не выдаёт себя за отказ.
- [x] 2.2 Проверить, что предупреждение не попадает в `violations` без
  указателя: по решению 0.13.8 пояснение живёт в `message`, а место — в
  `violations` только когда оно есть.

## 3. Сквозная проверка

- [ ] 3.1 Фикстура: пакет прежней ревизии, чьи стадии невозможность не
  объявляют, плюс `extend.yaml` со вставкой, которая объявляет. Проверка:
  компиляция проходит, предупреждение названо, прогон доходит до конца.
- [ ] 3.2 Прогнать `make check` и `make e2e`, читая **код самих ворот**, а не
  обвязки; записать точные счётчики.

## 4. Границы, которые изменение не закрывает

- [x] 4.1 Записать в `examples/troubleshooting.md`, что круг починки для вставок
  отсутствует: падение вставки может кончиться только `rejected`. Измерено
  зависимыми сессиями, не выведено из кода.
- [x] 4.2 Записать там же две соседние границы модели вставок:
  `project_extension_unknown_workflow` (вложенный граф не адресуется) и
  `project_extension_stage_conflict` (стадия именуется шагом).
