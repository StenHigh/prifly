## 1. Совместимость прежде функции

- [ ] 1.1 Тест на запечатанном графе ревизии 4 и 5: Run читается, компилируется
      и драйвится после изменения. Пишется и краснеет **до** расширения списка.
- [ ] 1.2 `verdictsRequiredBy(workflowVersion)`; `checkVerdictCoverage` берёт
      набор оттуда, а не из глобального `StepVerdicts`.

## 2. Вердикт

- [ ] 2.1 `blocked` в `StepVerdicts`: законен всегда.
- [ ] 2.2 Новая ревизия WorkflowRevision требует для него маршрут либо
      объявленную невозможность; граф без этого отказывается на компиляции.
- [ ] 2.3 Запечатанный граф 4/5 без маршрута: принятый результат сохранён,
      `unhandled_verdict`, Run падает с точным диагнозом. Проверить, что это
      прежнее поведение, а не новое.
- [ ] 2.4 `RoutedVerdicts` несёт новый вердикт хосту без правки текста раннера;
      если пин-тест раннера краснеет — значит текст всё же меняется, и тогда
      таблицы дайджестов двигаются тем же коммитом.

## 3. Читателю и автору

- [ ] 3.1 `examples/authoring/step-authoring-reference.yaml` и справочник
      ревизий: когда объявлять маршрут для `blocked`.
- [ ] 3.2 `examples/troubleshooting.md`: gate вернул `blocked` — что это значит
      и чего не значит.

## 4. Ворота

- [ ] 4.1 `make ci-check`, `make e2e`, `make race` зелёные; счётчики записаны.
- [ ] 4.2 Сообщить пакету: без возврата `blocked` из gate и маршрута в графе
      наблюдаемое изменение — только отсутствие ложного цикла исправления.
