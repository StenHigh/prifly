## История словаря

| Редакция | Изменение |
|---|---|
| 1.0 | Закреплены основные понятия, текущие aliases и границы F1/F2. По принятому ADR-0001 синхронизированы DATA-005 и OBS-007: разведены RunVersion, EventSequence и ReadCut, уточнены области CAS для control/publications. Runtime и wire-схемы не изменены |
| 1.1 | Добавлены понятия и bindings P2-01: capability manifest, InputConfiguration, EffectiveConfiguration, ProjectionManifest, отдельный профиль и durable on_error. Старые F1 JSON-поля и contract IDs сохранены |
| 1.2 | Разделены Guard и Predicate, определены Choice/ChoiceBranch, FieldRef, Operand, truth values и ChoiceDecision для P2-02. Уточнены snapshot semantics, ошибки и пределы AST; live guards и прежние Run/state/read contracts не расширены |
| 1.3 | Добавлены Call/WorkflowAlias и bindings настоящего WorkflowInvocation tree P2-03; уточнены scoped controls, closure configuration, partial, state/read v2 и capabilities/2. Старые contract IDs, histories и формальные статусы приёмки не изменены |
| 1.4 | Уточнён bounded Repeat P2-04: Iteration, RepeatProgress/RepeatDecision, независимые initial/next bindings, last-body exports, control charges, scoped recovery, local body alias до lock и state/read v3. Старые bundles и статусы формальной приёмки сохранены |
| 1.5 | Добавлены полные ContextManifest, typed context resources/profiles, SourceSnapshot/ContextRequest и отдельные CheckDefinition/CheckExecution/Request/Result P2-05. Core configuration2 явно выбирает state/read4; старые Runs и bundles неизменны. Наличие bindings не означает завершения автоматической приёмки результатов |
| 1.6 | Закреплены PendingAcceptance, элементы обязательных checks, EvidenceRef и separate process settlement/result acceptance для state4. Сохранены точные checked bindings, прежние ArtifactRef/Evidence wire forms и границы отдельной check telemetry; формальная приёмка не изменена |
| 1.7 | Добавлены AuthorityControl, SessionPrincipal/ObjectAccess и ControlPin для AIF-01/P2-06: управляющая область отделена от Run, допуск привязан к проверенной версии управления. Approvals, quorum, grants и waivers не введены; прежние Run/state/read bundles и формальные статусы приёмки не изменены |
| 1.8 | Добавлены SealedPackage, PackageComponent и TrustDecision для AIF-02/P2-07: явный импорт локального пакета отделяет получение, проверку и регистрацию, а доверие является authority-решением над exact manifest digest. Remote registry, автообновление, dependency closure пакетов и компоненты policy/tool/adapter не введены; опубликованные bundles и формальные статусы приёмки не изменены |
| 1.9 | Добавлен WorktreeClaim для AIF-03/P2-08: исключительное authority-владение одним Git worktree с generation, проверенным base commit, confined path и cleanup только собственной device/inode identity. Parallel scheduler, fairness queue и remote claims не введены; опубликованные bundles и формальные статусы приёмки не изменены |
| 1.10 | Добавлены AssistedSession и SessionHandoff для AIF-04/P2-09: шаг может исполняться уже существующей сессией, а Run записывает удостоверённого держателя попытки, выданные pinned skill refs, claimed worktree, часы срока и факт неявки хоста. Закрытие опирается на сам отчёт и объявленные outputs, без фактов процесса. Введены state/read версии 5 и седьмой public bundle; шесть прежних bundles остались байт-идентичными. Managed daemon, remote executor и квалификация provider не введены |
| 1.11 | Добавлены ControlApproval, ApprovalVote и authority-side control approval policy для второго среза P2-06: quorum и независимость замораживаются в решении, независимость сравнивает human_id, потребление однократно и атомарно с защищённым изменением, истечение окна вычисляется и не подделывается сохранённым переходом. Опубликованные Approval/ApprovalVote контракты используются без изменений |
| 1.12 | Добавлены ControlGrant и ObjectRead для третьего среза P2-06: ограниченная делегированная авторизация с запечатанными constraints и изменяемым учётом, расход в одной транзакции с защищённым изменением, выдача гейтится как делегируемая операция и не выдаёт прав, которых у subject нет. Чтение записей и telemetry фильтруется текущим object access до подсчётов |
| 1.13 | Добавлен QualityWaiver для четвёртого среза P2-06: записанный отказ требовать одну названную проверку, покрывающий ровно её; проверки осмысленности вне досягаемости; снижение качества остаётся видимым как `completed_with_waivers`. Введены state/read версии 6 и восьмой public bundle; семь прежних bundles байт-идентичны и теперь встроены и проверяются |
| 1.14 | Добавлен PackageStatus для P2-07: removed/quarantined закрывают разрешение, revoked блокирует и старые pins, ни один статус не удаляет bytes и не стирает факты внешних действий. Отзыв терминален и не отменяется повторным импортом; verify сообщает расхождение, не исправляя его |
| 1.15 | Добавлены TrustRoot и PackageSignature для P2-07: подпись доказывает, кто запечатал bytes, и отделена от runtime-квалификации; ключ внутри пакета не назначает себя доверенным; hash архива, digest манифеста и предмет подписи остаются раздельными. Архив читается как данные: traversal, links и коллизии отклоняют его целиком до создания файлов |
| 1.16 | Добавлены ResourceLease и ClaimProcess для первого среза P2-08: истёкший срок переводит владение в suspected и блокирует конкурирующий claim вместо передачи ресурса; владелец опознаётся PID вместе с clock session и временем старта; физические псевдонимы являются одним ресурсом |
| 1.17 | Добавлен ReportedCost: точная десятичная сумма от самоназванного источника сохраняется на Attempt, несколько источников не примиряются, отсутствие не становится нулём. Введены state/read/next версии 11, assisted-session/2 и тринадцатый public bundle; двенадцать прежних bundles остались байт-идентичными. Provider usage, rate cards, агрегация и hard monetary budget не введены |
| 1.18 | Добавлен ArtifactPublication первого среза P2-12: Core StepDefinition v3 объявляет typed artifact hook, `step.publish` v2 предъявляет exact workspace candidate, authority копирует и запечатывает bytes до записи logical item. Введены state/read/next/preview/step-read версии 12 и четырнадцатый public bundle; тринадцать прежних bundles и EventEnvelope v1 неизменны. Subscriptions, close/manifest и executable artifact checks не введены |
| 1.19 | Добавлен одноразовый путь публикационной подписки: StepDefinition v4 явно разрешает `declared_subscribers`, PublicationSourceDefinition связывает direct sibling producer с durable wait, а consumer получает exact sealed ArtifactRef до terminal producer. Run/state/EventEnvelope v12 не расширены; `each_publication`, cursor, close/manifest, blob delivery и generic `subscriptions` не введены |
| 1.20 | Добавлены ArtifactManifest и ArtifactClosure: `step.publish` v3 закрывает keyed-many hook только exact ordered membership, authority запечатывает полный manifest, повтор сохраняет первую запись, поздний item запрещён. Введены state/read/next/preview/step-read версии 13 и пятнадцатый public bundle; четырнадцать прежних bundles неизменны. Cursor, stream assignments, `Closed|Interrupted` delivery и generic subscriptions не введены |
| 1.21 | Добавлены bounded `each_publication`, PublicationSubscription, PublicationCursor, PublicationAssignment и PublicationDelivery: direct subscriber repeat переносит cursor через `iteration_output`, choice различает `Item|Closed|Interrupted`, а pending item остаётся закреплённым до settlement body. Введены WorkflowRevision v3, publication-source/2, state/read/next/preview/step-read версии 14 и шестнадцатый public bundle; пятнадцать прежних bundles неизменны. `new_only`, immediate producer-failure wakeup, blob delivery, backpressure/GC и generic subscriptions не введены |
| 1.22 | Добавлен Workflow authoring source `prifly-workflow/1`: ограниченный YAML однозначно опускается в прежнюю WorkflowRevision до compiler/lock, сокращает exact refs, ports, bindings и Predicate operands, поддерживает все восемь текущих stage kinds и сохраняет полную машинную форму как escape hatch. Wire-схемы, runtime semantics, исторические bundles и формальные статусы приёмки не изменены |
| 1.23 | Добавлены `publication-source/3` и `/4` с initial `new_only`: once/stream сохраняют authority event-sequence cut при регистрации и не доставляют publication/closure на этом cut или раньше. Введены state/read/next/preview/step-read версии 16 и восемнадцатый public bundle; retained v1/v2 и прежние bundles неизменны. Немедленный producer-failure wakeup, reservation, blob delivery, backpressure/GC и generic subscriptions не введены |
| 1.24 | Добавлены `publication-source/5` и `/6` с явным `interrupt_on_terminal_failure`: terminal failed producer направляет once wait по declared failure route или выдаёт stream `Interrupted` с причиной `producer_terminal_failed`; обработанная `on_error` ошибка не считается terminal failure. State/read/next/preview/step-read v17 сохраняют различие `producer_failed` и deadline interruption; все прежние source и bundles неизменны |
| 1.25 | Добавлены `publication-source/7` и `/8` с explicit JSON/blob delivery type и одним declared blob media type. Sealing по-прежнему проверяет blob descriptor hook schema, а consumer binding проверяет format/media type; state/read bundle не расширяется, поскольку durable Run shape не меняется. Source v1–v6 остаются JSON-only |
| 1.26 | Добавлены ActionIntent и ActionAdmission как разные F2-границы: v19 хранит admission exact proposal и approval consumption в одной authority transaction, но не создаёт delivery или внешний effect. Runs v18 и прежние bundles не расширяются; grant consumption для action ещё не заявлена |
| 1.27 | Core v20 добавляет один exact resource-scoped `action.admit` Grant к ActionAdmission: scope всех targets проверяется и Grant/обязательный Approval расходуются с admission атомарно. v19 остаётся approval-only; delivery и внешний effect не появляются |
| 1.28 | Core v21 добавляет ActionDelivery только в исходном `prepared/not_started`: durable запись связывает допущенную operation с executor до внешнего вызова. Она не выдаёт credential, не создаёт receipt и не заявляет effect; dispatch, retry и reconciliation остаются следующими отдельными переходами |
| 1.29 | Добавлены TaskInput/1 и local Task intake: exact выбранный текст и provenance запечатываются как SourceSnapshot, затем без изменения scope, criteria или confirmation выводится RunBrief. Provider adapters, credentials и внешняя запись не появляются |
| 1.30 | Добавлен Project package source: tracked YAML объявляет состав sealed package и явные compile values, а `project compile` выпускает его вне authority. Standard project profile нейтрален к продуктам; import остаётся отдельным authority-решением |
| 1.31 | Добавлена compact Project workflow folder: `workflow.yaml` несёт package header и root graph, optional `extend.yaml` и рекурсивные component directories остаются YAML source; compiler seal-ит raw context из declared project skill root без AI Factory dependency |
| 1.32 | Добавлен Step authoring source `prifly-step/1`: YAML source опускает безопасные defaults в неизменный StepDefinition v2 до schema validation и sealing; effects, executor, retry, result schema и повышенные capabilities остаются явными |
