# aif-docs: project context

Do not run in this repository: a `docs/` directory is forbidden by
`openspec/specs/release-documentation-layout/spec.md`. Documentation is
`README.md` (product entry), `examples/` (authoring references,
`troubleshooting.md`) and `openspec/specs/` (normative). Say so and stop.
