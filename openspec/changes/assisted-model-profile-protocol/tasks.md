## 1. Объявление в запечатанном плане

- [ ] 1.1 `internal/flow`: `StepDefinition` получает необязательное
  `model_profile` — `requested` (имя профиля работы) и `reason` (зачем он
  этому шагу). Новая авторская версия шага; пакет без объявления компилируется
  и ведёт себя как прежде. Профиль едет в `WorkflowRevision`, как
  `technical_retries` в 0.13.34, и не создаёт state-версии. Проверка: тест —
  пакет без поля компилируется байт-в-байт как раньше; с полем оно доезжает до
  плана.

- [ ] 1.2 Имя профиля — требование к работе, не провайдер. Отказ на значении,
  похожем на имя площадки или модели, с текстом, который называет разницу.
  Проверка: красный-до-правки тест на таком значении.

## 2. Задача

- [ ] 2.1 `SessionTask` несёт объявление, спроецированное из запечатанного
  плана при чтении, как `routed_verdicts`. `SessionHandoff` не меняется:
  новая редакция пинится только для задачи и отчёта, состояние остаётся на
  своей. Проверка: тест — задача шага с объявлением называет его; шаг без
  объявления отдаёт документ прежней формы.

- [ ] 2.2 Замер до правки записан в change: сколько state- и read-версий несёт
  `core-workflow/1` и каков объявленный потолок. Без этого числа выбор
  «не растить состояние» выглядит вкусовым.

## 3. Отчёт

- [ ] 3.1 `SessionSubmission` принимает заявление хоста: исполнил (и чем), не
  умеет, выбрал другое (и почему). Отчёт без заявления по шагу с объявленным
  профилем отказывается типизированным кодом. Проверка: три принимаемых
  формы и одна отказываемая, каждая своим тестом.

- [ ] 3.2 Заявление оседает в существующей диагностике Run как наблюдение
  хоста о себе. Ни один ответ движка не называет его подтверждённым выбором
  модели. Проверка: тест — ни `run status`, ни `session task`, ни `next` не
  содержат формулировки, утверждающей проверенный выбор.

## 4. Контракты

- [ ] 4.1 Новое семейство опубликовано рядом со старым, а не вписано в него:
  пять digest-пиннутых бандлов не меняются. `make schemas-check` сходится
  прежними sha256 на всех прежних бандлах.

- [ ] 4.2 Число state- и read-версий после изменения по-прежнему 32.
  Проверка: тест, который падает, если их стало 33 — потолок объявлен в
  опубликованном контракте и молча ломает capability-документ.

## 5. Документы и ворота

- [ ] 5.1 `examples/authoring/step-authoring-reference.yaml` и
  `examples/troubleshooting.md` называют поле, три исхода заявления и то, чего
  движок не обещает. Проверка: `python3 -B test/e2e/test_examples.py`.

- [ ] 5.2 Раннер называет объявление и три исхода: что делать, если площадка
  не даёт выбрать, — сказать честно, а не промолчать. Пины обновлены.

- [ ] 5.3 `openspec/specs/delivery-roadmap/spec.md`: запись очереди и
  требование о model profile обновлены по факту, без заявления product
  qualification. `aif-fanout` остаётся future acceptance fixture: успешная
  компиляция по-прежнему не доказывает выбор модели.

- [ ] 5.4 `make ci-check`, `make e2e`, `make race` зелёные; счётчики записаны
  здесь.
