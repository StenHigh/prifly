## 1. Состав окружения виден до запуска

- [x] 1.1 Семейство контрактов 33: ответ о следующем действии несёт для готовой программной стадии имена переменных и объявленные места; версия ставится только Run'у, который исполняет программы. Проверка: тест — Run с объявленным источником и с литералом: ответ называет имя и место, не содержит значения; `go test ./internal/runtime ./cmd/prifly -run 'Program|EnvironmentSource'`.
- [x] 1.2 Значение не печатается ни в одном выводе этой поверхности. Проверка: тест — вывод обходится на подстроку значения, как это уже делает тест запечатанного состояния.

## 2. Молчаливая правка перестаёт быть молчаливой

- [x] 2.1 `project local set` называет, что правка относится к следующему запуску, а не к уже начатым Run. Проверка: тест — вывод команды содержит это правило один раз и не обещает конкретных Run.

## 3. Отказ называет записанное

- [x] 3.1 `internal/runtime`: диагностика `nonzero_exit` несёт код завершения и границы записанного. Проверка: тест — программа с кодом 3: сообщение содержит `3` и говорит, что вывод программы не хранится; `go test ./internal/runtime -run 'Driver|Settle'`.

## 4. Документы и ворота

- [x] 4.1 `examples/troubleshooting.md`: записи про запечатанное окружение и про `nonzero_exit` приведены в соответствие реализованному. Проверка: `python3 -B test/e2e/test_examples.py`.
- [x] 4.2 `make ci-check`, `make e2e`, `make race` зелёные; счётчики записаны в change (`evidence.md`, 2026-09-18).
