## 1. Отдать находки

- [x] 1.1 Контракт шага v10: `required_for` принимает `blocked`, `maxItems`
      растёт вместе с набором. Тест держит обе половины: без второй контракт
      обещает список, который нечем заполнить.
- [x] 1.2 v3…v9 байт в байт прежние — проверено генерацией, не рассуждением.
- [x] 1.3 Анализ гарантий не трогался: читает `required_for` обобщённо.

## 2. Вставка

- [x] 2.1 Подъём вместо присваивания; отсутствующая версия задаётся,
      неизвестная остаётся.
- [x] 2.2 Отказ на месте склейки называет ревизию и её набор вердиктов.
- [x] 2.3 Тест по таблице ревизий, включая неизвестную. Поймал изъян в самой
      правке: `slices.Index` даёт −1, и сравнение объявляло неизвестную ревизию
      старше всех, то есть понизило бы её.

## 3. Объявление и ворота

- [x] 3.1 `step_definition_versions` содержит `10`; тест держит это вместе с
      ревизией 6 и обеими версиями `core:schema/step-result`.
- [x] 3.2 `make ci-check` зелёный: fmt-check 321 файл, refusal-check 170.

## 4. Найдено выпуском 0.13.51

- [x] 4.1 Контракт принимал вердикт, а **авторский путь** — нет. Источник
      опускается до контракта, который несут его остальные поля, и порт того
      контракта слова не знал. Лестница вывода теперь поднимает шаг до v10,
      когда любой `required_for` называет `blocked`, а оба авторских маркера
      допускают v10 явно.
- [x] 4.2 Мой тест шёл прямо через `ProtocolSchema` и мимо понижения — ровно
      ловушка, которую я сам описывал у приёма результата, и она пропустила
      выпуск. Проверка теперь идёт через `lowerStepAuthoring`, и вырезание
      лестницы даёт `lowered to 2, expected 10`.
- [x] 4.3 Справочник авторинга перечислял в `required_for` шесть значений и
      пинован на `schema_version: "9"`; оба поправлены, тест справочника
      требует v10 и проверяет, что вердикт пережил понижение.
- [x] 4.4 Редакторская схема `step-v2` допускала `6…9`; обе копии — внутренняя
      и опубликованная — теперь допускают `10` и совпадают байт в байт.
- [x] 4.5 Два теста пиновали «10» как ещё не известную сборке редакцию, с
      комментарием, что 9 ею быть перестала. Сдвинуты на «11», причина дописана.
- [x] 4.6 `make ci-check` зелёный: fmt-check 321 файл, refusal-check 170.

## 5. Найдено выпуском 0.13.52

- [x] 5.1 Шестая копия списка версий шага — `switch` в `project compile`,
      обрывающийся на `"9"`. Шаг, опущенный до 10, падал в **базовый**
      контракт, у которого порт знает шесть вердиктов. Отсюда и дословно тот же
      отказ, и безразличие к явному пину: пин доводил версию до 10, а
      сопоставление её не знало.
- [x] 5.2 Сопоставление версии шага с контрактом получило один дом:
      `flow.StepContractFor`. Неизвестная версия отвечает пустым, а не базовым
      контрактом «который случайно принял»: решать, что с ней делать, —
      вызывающему.
- [x] 5.3 Проверка входит там, где входил пакет: `project compile` над
      источником с `{{…}}`-подстановками, то есть их вторая гипотеза
      (плейсхолдеры) снимается тем же прогоном, а не рассуждением. Возврат
      сопоставления к «9» воспроизводит их отказ дословно.
- [x] 5.4 Страховка от седьмой копии: список версий держится против мутаторов,
      которые эти контракты производят, и против каждой ступени лестницы
      авторинга.
- [x] 5.5 `make ci-check` зелёный: fmt-check 322 файла, refusal-check 170.

## 6. Подтверждено независимо (2026-09-25)

- [x] 6.1 Сессия пакета прогнала все четверо ворот на опубликованном 0.13.53,
      пять пакетов, авторский путь с подстановками: шаги verify/security/review
      запечатаны как v10, profiled-варианты с `model_profile` тоже v10.
- [x] 6.2 Корень и review-once — ревизия 6, `blocked` уходит в finish с
      артефактом гейта, `unavailable_output` не возникает. Графы без гейтов
      остались на v4 рядом с v6 в одном пакете — смешение ревизий работает.
- [x] 6.3 Совместимость вставок проверена с обеих сторон: вставка в корень v6
      сначала упала `missing_handler at …/on/blocked`, как и должна, а с
      `impossible_verdicts: [blocked]` прошла — 5 запусков, 4 сборки,
      `core-state/35`. Понижения графа до v4 нет.
- [x] 6.4 Здесь же проверено на чужом стенде: минимальный пакет с шагом формы
      пакета, `project compile` на опубликованном бинаре запечатал
      `schema_version: "10"` и `required_for: [pass, needs_revision, blocked]`.
      Побочно: граф, маршрутизирующий `blocked`, обязан быть ревизии 6, иначе
      отказ приходит на `kind` стадии и читается не про вердикт.
