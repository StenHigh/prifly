# aif-roadmap: project context

The roadmap of this repository is `openspec/specs/delivery-roadmap/spec.md`, a
normative OpenSpec spec: it changes only through an OpenSpec change and sync.
Do not create, edit or trim any roadmap file here; tell the user to open a
change (`/openspec-propose`) instead, and stop.
