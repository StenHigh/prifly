# aif-architecture: project context

`.ai-factory/ARCHITECTURE.md` is a map of the existing packages and entry
points, not a pattern recommendation. Architectural decisions live in
`openspec/specs/architecture-decisions/spec.md` and change only through an
OpenSpec change. Update the map only when the layout changed; never rewrite it
from a template, and never touch the roadmap.
