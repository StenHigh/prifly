## 1. Бюджет

- [x] 1.1 `RegistryBudget.WouldRefuse` заполняется `dependency_limit` тем же
      сравнением `>`, которым отказывает `localRegistry`.
- [x] 1.2 Тесты: обзор при превышении называет отказ, при равенстве границе —
      нет. Вырезание сравнения краснеет на превышении, замена `>` на `>=` —
      ровно на границе: обе стороны проверены, а не одна.

## 2. Source продолжения

- [x] 2.1 Отказ от Run-продолжения называет исходный Run по цепочке `fork`,
      обход ограничен по числу шагов.
- [x] 2.2 Тесты: продолжение от продолжения называет исходный Run; обрыв
      цепочки, чужая причина fork и цепочка длиннее обхода не называют ничего;
      цикл не вешает чтение. Снятие границы вешает пакет на таймаут, и это
      единственный способ показать, ради чего граница есть.

## 3. Ворота

- [x] 3.1 `make ci-check` зелёный: fmt-check 317 файлов, refusal-check 170
      файлов, бандлы схем без изменений.
