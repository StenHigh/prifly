Порядок фаз — порядок приоритета. Каждая фаза поставляется своей серией
коммитов с зелёным `make ci-check`; фаза не начинается, пока предыдущая не
имеет записанного evidence. Все команды выполняются из явного target worktree
(`git -C`, абсолютные пути). Проверка защищённой истории общая для каждой
задачи: `git diff --name-only -- openspec/changes/archive` пуст, published
bundles и historical evidence не изменены.

## 1. Обязательства и recovery (корректность)

- [ ] 1.1 Добавить control operation `run.resolve` в `ownerOperations` и reconcile (`internal/runtime/authority.go`); новые event types `attempt.resolved`, `check.resolved` в `EventTypes`; словарь получает термин «Resolution — Разрешение неопределённого обязательства» с Go/JSON-соответствиями; проверить `go test ./internal/runtime -run '^TestGlossaryBindings$' -count=1` и `make schemas-check`.
- [ ] 1.2 Реализовать `Engine.ResolveObligation` и `prifly run resolve RUN_ID (--attempt|--check) --outcome not_applied|applied --reason TEXT [--command-id]`: transform переводит attempt/check в `failed` с `resolved_*`, закрывает activation/step/invocation без `routeKnownError`, пересчитывает `HasUnresolvedEffects`, освобождает slot через `ReleaseSlot`; отказ при удерживаемом `driver.lock`; проверить новый тест `TestResolveReleasesSlotAndNeverRoutesOnError` (crash после `start_returned` → uncertain → resolve → `capacity show` не содержит slot → новый Run стартует) и `TestResolveRefusesLiveDriver`.
- [ ] 1.3 `MarkSessionDisconnected` (`sessions.go:630`) переводит attempt/step/activation в `uncertain`; `interruptDriver` не оставляет dispatched attempt без диагностики; проверить `go test ./internal/runtime -run 'TestSession.*Disconnect|TestDriverInterrupt' -count=1`.
- [ ] 1.4 Recovery по сохранённым доказательствам (`driver.go` ветка `active`, `check_execution.go`): при `ExecutorEnd != nil` закрывать attempt/check как `failed(executor_settlement_lost|check_settlement_lost)` с `ReleaseSlot`; при `Started == nil` или `ExecutorEnd == nil` — прежнее uncertain; проверить новые тесты `TestDriverCrashAfterGroupEmptySettlesWithoutUncertainty` и существующий `TestDriverCrashAfterActualStartRetainsUncertaintyAndSlot` (граница сохраняется).
- [ ] 1.5 `_busy_timeout` по умолчанию 3 с (`internal/local/store.go:280`); `observe`/`observeCheck` повторяют `Apply` при `sqlite3.ErrBusy` внутри своего контекста; новый settlement code `authority_persistence_failed`, исключённый из `routeKnownError`; проверить тест с удержанным write-lock из второго процесса во время `group_empty`: шаг не получает `executor_observation_failed` и workflow не входит в `on_error`.
- [ ] 1.6 `settleWith` (`driver.go:1283–1313`) сохраняет исходную ошибку в bounded поле `Diagnostic.Detail` и классифицирует infrastructure-ошибки (`local.ErrIntegrity`, `*os.PathError`, `ErrBlobLimit`, storage budget) отдельным кодом `authority_output_sealing_failed`, не входящим в `on_error`; проверить тест с недоступным blob store при settlement и `run status`, показывающий причину.
- [ ] 1.7 Идемпотентность: убрать `expires_at` из payload `Waive` (`waivers.go:86`), убрать `ObservedAt` из содержимого vote artifact (`approvals.go:315`, время в Approval record); добавить общий тест `TestEveryCommandRetryIsDuplicate`, прогоняющий все mutable CLI/API команды дважды с одним `command-id`; проверить `go test ./internal/runtime ./cmd/prifly -run 'Retry|Duplicate' -count=1`.
- [ ] 1.8 `aggregateSchemaRef` (`internal/flow/calls.go:119`) выбирает exact builtin version детерминированно (sorted или явная версия); проверить тест с двумя версиями `core:schema/aggregate-manifest` в registry, дающий одинаковый результат при 100 повторах.
- [ ] 1.9 Evidence фазы: `make ci-check`, `make e2e`, `openspec validate harden-authority-reliability-and-performance --strict`; зафиксировать результаты здесь; `git diff --name-only -- openspec/changes/archive` пуст.

## 2. Чистые транзакции

- [ ] 2.1 Guard чистоты transform: счётчик `local.transformDepth` вокруг вызова transform в `Apply`/`ApplyAuthority`/`CreateLinkedRun`; в тестовой сборке `runSchemaWorker`, `readLocal`, `BlobStore.Put/Read/Export/Inspect`, `exec.Command` и `os.Open*` в `internal/runtime` проваливают тест при `depth > 0`; проверить `go test ./... -count=1` — все нарушения из 2.2–2.4 найдены тестами до исправления.
- [ ] 2.2 Вынести компиляцию/чтение из transform'ов: `actionProposalAttempt` (`actions.go`, вызывается из `ProposeSessionAction`/`AdmitSessionAction`), `acceptance.go:321`, `publication_checks.go:207,221`, `approvals.go:359`; в transform передаются заранее вычисленные plan digest, artifact refs и результаты проверок; проверить targeted tests `TestAction|TestAcceptance|TestPublicationChecks|TestApproval` и guard из 2.1.
- [ ] 2.3 `chargeInvocation` (`invocation.go:376`): убрать `supportedRun` из пути бюджета, считать `invocationLimits` один раз на команду (кэш лимитов по WorkflowRef внутри apply), не парсить workflow повторно; `invariant()` не повторяет инварианты, уже выполненные `load`, в той же команде; проверить `go test ./internal/runtime -run 'Invocation|Budget|Invariant' -count=1` и benchmark из 8.1.
- [ ] 2.4 Бюджет публикаций по накопительному счётчику `OptionalBytes` в Run вместо `canonicalState` всех публикаций (`publications.go:278`, `artifact_publications.go:97`, `artifact_closures.go:182`); `checkCounterProgress` (`publications.go:328`) хранит последнее числовое значение счётчика в Publication вместо повторного парса всех прежних; проверить `go test ./internal/runtime -run 'Publication|Counter|Budget' -count=1`.
- [ ] 2.5 Телеметрия команды в той же транзакции: `Change.Samples`/`AuthorityChange.Samples`, `recordCommand` (`measurements.go:13`) без второй транзакции; `AppendSamples` использует `MAX(seq)`, один запрос на батч по `id IN (...)` и prepared statements; `Store.Apply` использует prepared statement для вставки событий; проверить `go test ./internal/local -count=1` и `TestTelemetrySamplesRecordedWithCommand`.
- [ ] 2.6 Evidence фазы: `make ci-check`, `make e2e`, `openspec validate harden-authority-reliability-and-performance --strict`; `git diff --name-only -- openspec/changes/archive` пуст.

## 3. Валидация схем in-process

- [ ] 3.1 `internal/flow/schema_worker.go`: `checkSchema` компилирует схему in-process (`newSchemaCompiler`, `noExternalSchema`, Draft2020) с LRU-кэшем `*jsonschema.Schema` по digest bytes (≤ 64 записей / 8 МиБ) и валидирует значение in-process; подпроцесс сохраняется за `PRIFLY_SCHEMA_WORKER=1`; проверить `go test ./internal/flow -count=1`, тест на отсутствие `os/exec` вызова при валидации и fuzz `FuzzValidateJSON` на схемах из `test/fixtures/contracts`.
- [ ] 3.2 Кэш `ProtocolSchema(name)` и `PublicationSourceSchemaV1–V8` (один параметризованный builder вместо четырёх копий); `sourceBuiltinDefinitions` берётся из `Builtins()` вместо повторного построения; `ValidateSchema` переиспользует кэш вместо нового `Plan{}`; проверить `go test ./internal/flow ./internal/runtime -run 'Schema|Source|Builtin' -count=1`.
- [ ] 3.3 `decisionInvariant`/`ValidateDecisionSheet` при `load` проверяют только структуру; значения валидируются при записи (Start, Answer, Request); проверить тест, что `load` Run v25 с decisions не вызывает валидатор (счётчик), и `TestDecision.*`.
- [ ] 3.4 `collectPublications` (`telemetry.go:1415`) сверяет digest публикации вместо повторной `ValidatePublication`; `sealCollection` (`map.go:65`) валидирует элементы in-process; проверить `go test ./internal/runtime -run 'Telemetry|Map' -count=1`.
- [ ] 3.5 Измерить: `verify-core.py` и `go test ./internal/runtime` до/после; записать числа здесь; удалить fallback подпроцесса в отдельной задаче после release с evidence.
- [ ] 3.6 Evidence фазы: `make ci-check`, `make e2e`, `openspec validate harden-authority-reliability-and-performance --strict`; `git diff --name-only -- openspec/changes/archive` пуст.

## 4. Хранилище

- [ ] 4.1 Storage version 5: столбец `authority.verified_cut`; `OpenStore` проверяет заголовок/идентичность, инкрементально верифицирует `cut > verified_cut` и сдвигает отметку; `quick_check` только при `verified_cut == 0`; `Store.Verify` (полный скан) остаётся для `doctor`; миграция v4→v5 идемпотентна при остановленных admissions; проверить `go test ./internal/local -run 'Open|Verify|Migrate' -count=1`, тест прерванной миграции и benchmark `BenchmarkOpenStore10MB` (цель ≤ 20 мс).
- [ ] 4.2 Таблица `pinned_bytes(digest, bytes)`; store заменяет `definitions[].bytes`, `context_resources[].bytes`, `attempts[].envelope` ссылками по digest при записи и восстанавливает при чтении с проверкой прежнего `snapshot_digest`; старые снапшоты читаются без преобразования; проверить round-trip тест, `TestStoreVerify` на смешанной БД и размер БД `verify-core.py` (цель ≤ 2 МБ).
- [ ] 4.3 Переходы в journal: событие `state.changed` вместо `Run.Transitions` (новая state version), `Timing`/`check_telemetry` читают переходы через `Store.Read`, старые Runs — из поля снапшота; сгенерировать bundles через `make schemas`; проверить `go test ./internal/runtime -run 'Timing|Transition|Compat' -count=1` и `make schemas-check`.
- [ ] 4.4 Fsync: `_synchronous=NORMAL` для WAL с записью trade-off в `runtime-resources` failure model; `putArtifact` проверяет существующую identity один раз, без `dir.Sync()` на дубликате; `release.replace` — fsync родительского каталога; проверить `go test ./internal/local ./internal/runtime ./internal/release -run 'Artifact|Blob|Replace' -count=1` и sys-time `verify-core.py` (цель ≤ 1,5 с).
- [ ] 4.5 `ReadAllAt(cut=-1)` читает `runs`; partial index `events(run_id, seq) WHERE state_after IS NOT NULL` для исторических cut; `readProcess` на Linux использует `bytes.LastIndexByte`/`bytes.Fields`; проверить `go test ./internal/local -count=1` и `EXPLAIN QUERY PLAN` без full scan в тесте.
- [ ] 4.6 Evidence фазы: `make ci-check`, `make e2e`, `openspec validate harden-authority-reliability-and-performance --strict`; измерения из design-таблицы записаны здесь; `git diff --name-only -- openspec/changes/archive` пуст.

## 5. Горячие циклы драйвера

- [ ] 5.1 Кэш планов `Engine.plans` по `(WorkflowRef, resources snapshot digest)`; `r.plan()` через Engine; `Telemetry` использует общий кэш; проверить тест-счётчик компиляций за `Drive` (≤ числа различных workflow) и `go test ./internal/runtime -run 'Plan|Drive' -count=1`.
- [ ] 5.2 Общий watchdog для attempt и check (`driver.go:884`, `check_execution.go:424`): опрос `SELECT version, event_seq FROM runs` каждые 250 мс, `load` только при изменении; проверить `go test ./internal/runtime -run 'Watch|Cancel|Deadline' -count=1` и отсутствие полной загрузки в idle (счётчик).
- [ ] 5.3 `RunProcess`: адаптивный интервал опроса группы (20 мс первые 200 мс, затем 200 мс), `readProcess` без `string(data)` копий; проверить `go test ./internal/local -run 'Process' -count=1` и профиль CPU идle-шага (цель ≤ 2 % ядра).
- [ ] 5.4 Кэш `ProcessExecutableDigest` по `(dev, ino, size, mtime, ctime)` на время процесса; `Start` хеширует только executor'ы плана и каждый distinct executable один раз; проверить тест подмены файла (stat изменился → повторный хеш, digest mismatch отказ) и `go test ./internal/runtime -run 'Executable|Digest' -count=1`.
- [ ] 5.5 Индексы Run на время загрузки (activations по invocation, invocations по caller, transitions по (kind,id), checks по (invocation, activation), publications по (attempt, hook)); использовать в `invocationInvariant`, `Timing`, `stepReadView`, `recordDiagnostic`, `guardTargetOpen`, `parallelBranches`, `repeatBodies`; `contextPinnedInvariant` кэшируется по digest снапшота; проверить benchmarks `BenchmarkLoad1000Invocations`, `BenchmarkTiming1000Nodes` (цель ≤ 5 мс) и полный `go test ./internal/runtime -count=1`.
- [ ] 5.6 Кэш `Artifact` metadata и `inventoryResources` на время Engine (неизменяемые по digest); `prepareBindingsForBody` читает каждый input один раз; `FireDueSlots` считает `live` один раз и обновляет инкрементально; `projectStart` открывает engine один раз; проверить `go test ./internal/runtime ./cmd/prifly -run 'Artifact|Inventory|Schedule|ProjectStart' -count=1`.
- [ ] 5.7 Evidence фазы: `make ci-check`, `make e2e`, benchmark-отчёт из 8.1 до/после; `git diff --name-only -- openspec/changes/archive` пуст.

## 6. Архитектура кода

- [ ] 6.1 `stateRank`/`atLeast` и таблица `versionContracts` заменяют 24 предиката и семь копий лестницы (`engine.go:290,600`, `publications.go:420`, `start.go:390,1000`, `driver.go:452`, `compatibility.go:96`); удалить мёртвые `Core*PreviewVersion`, не назначаемые в `Preview`; проверить golden-тест «версия для каждого state → одинаковый read/next/preview/step-read во всех точках» и `make schemas-check`.
- [ ] 6.2 Тип `runtime.Fault{Code, Message}` вместо `errors.New("code: msg")` (158 мест); `ProblemFor` без `strings.Cut(err.Error())`; CI-ворота `rg -n 'errors\.New\("[a-z_]+:|fmt\.Errorf\("[a-z_]+:' internal cmd` пусты; проверить `go test ./cmd/prifly -run 'Problem|Exit' -count=1`.
- [ ] 6.3 `local.IsPersistenceFailure` вместо импорта `go-sqlite3` в `runtime/problem.go`; `CGO_ENABLED=0 go vet ./...` добавлен в `ci-check`; spike `modernc.org/sqlite` — отдельный ADR с измерениями (не в этом change); проверить `make ci-check`.
- [ ] 6.4 Таблица команд CLI с признаком `mutates` вместо строковых `if` в `main.go:214–255`; общая реализация `stringsFlag`/parse; проверить `go test ./cmd/prifly -count=1` и тест «каждая mutable команда открывает authority на запись, каждая read-only — на чтение».
- [ ] 6.5 Дубли и мелочи: `itoa`/`contains` → `strconv`/`slices`; `_ = intentBytes`; `rPolicy` возвращает ошибку; `strings.NewReader(string(data))` → `bytes.NewReader`; `materializeTaskInput` в `os.TempDir()`; именованные константы для `3.51.3/3.50.7/3.44.6`, `71`, `30000`; `worktrees.go:585` `--end-of-options`; проверить `make vet fmt-check` и targeted tests.
- [ ] 6.6 `schema-gen`: table-driven контракты вместо 38 флагов и 26 полей; проверить `make schemas` без diff (`make schemas-check`).
- [ ] 6.7 Evidence фазы: `make ci-check`, `openspec validate harden-authority-reliability-and-performance --strict`; `git diff --name-only -- openspec/changes/archive` пуст.

## 7. Безопасность и поставка

- [ ] 7.1 `scripts/install.sh` скачивает `release-manifest.json` того же Release, сверяет SHA-256 архива до установки и отказывает при несовпадении; SECURITY.md называет HTTPS GitLab как единственный trust boundary первой установки; проверить `python3 -B test/e2e/verify-release-ci.py` и sh-тест с подменённым архивом.
- [ ] 7.2 Подпись manifest по RFC 8785: `release.Build` пишет `release-manifest.sig` (legacy) и `release-manifest.jcs.sig`; `Update` проверяет JCS-подпись; `verify-release-ci.py` требует обе на два release; проверить `go test ./internal/release -count=1`, тест старого updater против нового manifest и `make release-ci-check`.
- [ ] 7.3 Monitor: allowlist `Host` (`127.0.0.1:<port>`, `localhost:<port>`), `X-Content-Type-Options: nosniff`, ошибки через `ProblemFor`, `/api/artifact` читает ≤ 1 МиБ потоком; проверить `go test ./cmd/prifly -run 'Monitor' -count=1` с запросом с чужим `Host` → 421/403.
- [ ] 7.4 `validIdentity` для rejection code, event type и authority key — грамматика идентификатора (`^[a-z][a-z0-9_]{0,63}$` для кодов); `compareVersions` сравнивает numeric prerelease identifiers по semver и hoist regexp; проверить `go test ./internal/local ./internal/release -count=1`.
- [ ] 7.5 Evidence фазы: `make ci-check`, `make release-ci-check`, `openspec validate harden-authority-reliability-and-performance --strict`; `git diff --name-only -- openspec/changes/archive` пуст.

## 8. Ворота качества

- [ ] 8.1 Benchmarks с fixture БД (10 МБ и 100 МБ, генерируются детерминированно): `BenchmarkOpenStore`, `BenchmarkRunStatus`, `BenchmarkAdmit`, `BenchmarkPublish`, `BenchmarkTelemetry1000Runs`, `BenchmarkLoad`; результаты с hardware/software profile и durability settings публикуются в `openspec/changes/.../evidence.md` (per `quality-and-acceptance`); проверить `go test ./internal/... -run '^$' -bench . -benchtime 3x`.
- [ ] 8.2 Регрессионные тесты на каждый дефект ревью, не покрытый выше: crash после `group_empty`, busy-lock при `observe`, retry `waive`/`approval decide`, `resolve` освобождает slot, aggregate ref с двумя версиями, `decisionInvariant` без валидатора при load, telemetry на 1000 Runs в дедлайн; проверить `go test ./internal/runtime -run 'Regression' -count=1`.
- [ ] 8.3 Время тестов: `go test ./internal/runtime` ≤ 180 с после фаз 3 и 5; убрать 20 мс/30 мс ожидания из тестов через инъекцию интервалов; проверить `time make test`.
- [ ] 8.4 Документация: CLI help (`run resolve`), словарь (Resolution, verified cut, pinned bytes), `runtime-resources` failure model (`synchronous=NORMAL`), roadmap active backlog; проверить `openspec validate --specs`, `go test ./internal/runtime -run '^TestGlossaryBindings$' -count=1`.
- [ ] 8.5 Финальные ворота change: `make check`, `make e2e`, `openspec validate harden-authority-reliability-and-performance --strict`, `git diff --check`; записать фактические результаты и достигнутые метрики из design-таблицы; `git diff --name-only -- openspec/changes/archive` пуст; закрытие roadmap milestone или product gate не объявляется.
