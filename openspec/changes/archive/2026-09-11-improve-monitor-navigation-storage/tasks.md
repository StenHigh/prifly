## 1. Навигация и объём

- [x] 1.1 Реализовать отдельный экран Run, возврат к сохранённому списку и hash history; проверить связность DOM/JS и отсутствие синтаксических ошибок.
- [x] 1.2 Измерять размеры authority фоном отдельно от Run polling; проверить hardlinks, symlink и общий итог в TestMonitorStorageCountsHardlinksAndSkipsSymlinks.
- [x] 1.3 Вручную проверить длинный список, возврат/Back, размеры, preview/cancel/confirm в браузере. Сделано 2026-09-08 во встроенной панели: список 1/8 по 50 строк, возврат с page 3 восстанавливает страницу, фильтры и `scrollY`, размеры по хранилищам с итогом, предпросмотр с полным списком id, отмена ничего не удаляет, подтверждение по тому же запросу удаляет 32 Run (385 → 353) и отвергает устаревший digest. **Закрыто 2026-09-10:** щелчок сделан в видимой панели браузера. Диалог
  «Подтверждение очистки» показал путь авторитета, «Будет удалено Run: 7, файлов
  и каталогов: 65» и полный список из семи id; нажатие «Удалить без возможности
  отмены» закрыло диалог, и `GET /api/runs?page=1` ответил `total: 0` при
  источнике `indexed: true` без ошибки. Настройки, определения, workflow и пакеты
  авторитета остались на месте. Подробности — в [evidence.md](evidence.md).

  Наблюдение сделано на **одноразовом** авторитете, поднятом для этого
  (`scratchpad/uidemo/project`, семь прогонов, один источник). Монитор владельца
  на 7783 для этого не годился: он сканирует 1134 прогона по всей машине, включая
  переданные стенды `~/.prifly-stands/aif-classic*` и авторитеты чужого проекта,
  а подтверждение там необратимо.

## 2. Безопасное удаление

- [x] 2.1 Зафиксировать подтверждённый владельцем scope: массовое удаление только окончательно завершённых Run и полноценный GC; проверить согласованность трёх delta specs.
- [x] 2.2 Реализовать exclusive maintenance, атомарное удаление history/audit и GC с сохранением общих данных; проверить TestMonitorMaintenanceRefusesAndReclaims и Store.Verify.
- [x] 2.3 Добавить защищённый POST и полное подтверждение в UI; проверить отказ чужому Origin/неверному token/GET, stale plan и positive HTTP deletion.
- [x] 2.4 Проверить active/uncertain/pending states, открытый Engine, shared artifacts, imports, orphan uploads, повторы и восстановление служебного workspace после ошибки, не меняя symlink target.

## 3. Завершение

- [x] 3.1 Завершить race/vet/schema/OpenSpec проверки, записать evidence и убедиться, что historical release evidence не менялось.
