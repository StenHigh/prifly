# Проверка восстановления

Дата: 2026-09-12. Проверено в основном checkout на базе `9a4ba4d`.

- `go test ./cmd/prifly -run '^TestMonitor' -count=1`: PASS (4.149 с). Первый прогон в sandbox не смог открыть loopback-порты; повтор с доступом к loopback прошёл полностью.
- `node --test cmd/prifly/monitor_ui_test.cjs`: PASS, включая восстановленные проверки выбора до первого отображения, сохранения темы, изменения системной темы и запрета localStorage.
- JS syntax checks, `make build fmt-check refusal-check`, проверка уникальности HTML id и связей JS/DOM, `git diff --check`: PASS.
- `openspec validate restore-monitor-design --strict`: PASS.
- Chromium: PASS для загрузки эмблемы и панорамы, светлой/тёмной/системной темы, сохранения выбора после reload, перехода из списка в граф Run и обратно, ширины 390 px без горизонтального переполнения страницы, фокуса на переключателе. Ошибок JS нет. Просмотрены снимки обеих тем, подробностей Run и узкого экрана.

Браузер проверял новую сборку на `127.0.0.1:7783`, читая существующие Run. Удаление реальных данных не выполнялось. Полный release gate не запускался: изменение касается оформления, ресурсов и выбора темы.
