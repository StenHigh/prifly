# Проверки изменения монитора

Дата: 2026-09-08. Worktree: `/private/tmp/prifly-monitor-storage`.
Ветка: `codex/monitor-storage`, база: `a5a2202`.

## Выполнено

- `go test -race ./cmd/prifly ./internal/runtime ./internal/local -run 'Monitor|Cleanup|Storage|Fork|Start|Neutral|Grant|Approval|Claim|Glossary' -count=1`: PASS во всех трёх пакетах (102.707 / 164.691 / 4.987 с). Go 1.27, darwin/arm64.
- Точечные новые tests без race: PASS. Проверены запрет незавершённых Run и открытого Engine; сохранение общего артефакта другого Run и независимого import; удаление orphan blob/upload; stale preview; HTTP Origin/token/method и успешное удаление; повтор; восстановление служебного workspace из audit после ошибки файлового удаления; сохранность внешней цели symlink; `Store.Verify` после очистки.
- `TestMonitorStorageCountsHardlinksAndSkipsSymlinks`: PASS; выделенные блоки, hardlinks и общий итог без повторного учёта inode, отсутствие обхода внешнего symlink.
- `make vet fmt-check refusal-check schemas-check build`: PASS, включая CGO=0 vet. Публичные schema bundles не изменены.
- `node --check cmd/prifly/monitor.js` и `node --test cmd/prifly/monitor_ui_test.cjs`: PASS. Существующие UI tests проверяют escaping, timing, графы и file evidence; ручной проверки новых экранов они не заменяют.
- Статическая проверка уникальности HTML id, связности JS/DOM и структуры CSS: PASS.
- `openspec validate improve-monitor-navigation-storage --strict` и strict validation трёх затронутых specs: PASS.
- `git diff --check`: PASS. Исторические release evidence и manifests не менялись.

## Наблюдение в браузере — 2026-09-08, вечер

Монитор `prifly monitor --addr 127.0.0.1:7788 --scan-root <scratchpad>` открыт
во встроенной панели браузера; 385 Run из 44 хранилищ, фикстура — 68 прогонов
демо-проекта во временном каталоге.

- **Длинный список.** `1 / 8`, ровно 50 строк на странице; «Далее →» доводит до
  страницы 3 без перезагрузки.
- **Возврат.** С page 3 при `scrollY = 4200` открыт экран Run
  (`#source=…&run=…`, отдельный экран с «← Все прогоны»), затем `history.back()`:
  вернулись `3 / 8`, 50 строк, `scrollY = 4200`. То же с фильтрами
  `status=completed` + поиск `transform` (`Найдено: 33`, `1 / 1`) — после
  возврата оба фильтра, счётчик, страница и позиция на месте.
- **Размеры.** По каждому хранилищу «Занято / артефакты / доступно на диске»,
  общий итог 279,9 МиБ, время измерения и оговорка про inode и APFS clones.
  Область поиска и исключения названы; хранилища вне scan-root показаны из
  запомненных источников, как и описано.
- **Предпросмотр.** Диалог называет путь, `Будет удалено Run: 32`, число файлов
  и каталогов, объём содержимого, полный список id и текст о том, что
  сохраняется. Защищённых Run в этой фикстуре — 0.
- **Отмена.** Закрывает диалог с `returnValue = "cancel"`; `total` остался 385 —
  не удалено ничего.
- **Подтверждение** (по тому же пути, который вызывает кнопка): устаревший
  digest → `409 cleanup_plan_changed`; действительный → `200`, удалено 32 Run,
  384 файла, 396 770 байт, без warnings. Список: 385 → 353, источник
  демо-проекта остался в списке хранилищ.

Что осталось не наблюдённым и почему: панель браузера в этой сессии — скрытый
документ (`document.visibilityState === "hidden"`), а скрытый документ не
доставляет задачу события `close` у `<dialog>`. Проверено прямо: `dlg.close(
"cancel")` выставляет `returnValue`, закрывает диалог и **не** вызывает
слушателя `close`, добавленного строкой выше. Продолжение обработчика
(`await confirmation` → удаление или «Очистка отменена.») поэтому не
наблюдалось нажатием кнопки, и повторное открытие диалога в скрытой панели
не проходит из-за незакрытого `maintenanceBusy`. **Это ограничение среды, а не
дефект продукта**: серверная часть подтверждения проверена по тому же
запросу, что шлёт кнопка. Остаётся один щелчок в видимом окне.

## Подтверждение нажатием — 2026-09-10

Оставалась ровно одна ненаблюдённая вещь: продолжение обработчика после нажатия
кнопки подтверждения. В скрытом документе `<dialog>` не доставляет событие
`close`, поэтому 2026-09-08 её увидеть не удалось. Сделано в **видимой** панели
браузера.

```
диалог «Подтверждение очистки»
  путь авторитета:  …/scratchpad/uidemo/project
  «Будет удалено Run: 7, файлов и каталогов: 65 (35 КиБ содержимого,
   без оценки освобождения SQLite)»
  полный список из семи id
нажатие «Удалить без возможности отмены»
  диалог закрылся
  GET /api/runs?page=1 → total 0, источник indexed: true, error: null
  каталог авторитета: brief.json definitions.json inputs packages
                      prifly.json schemas scripts steps workflows
```

То есть настройки, определения, workflow и пакеты пережили очистку, а источник
остался читаемым — он не выпал из обхода и не сообщил об ошибке.

**Наблюдение сделано на одноразовом авторитете, поднятом ради него**: семь
прогонов, один источник, каталог в `scratchpad`. Монитор владельца на порту 7783
для этого не годился и не годится впредь — он сканирует 1134 прогона по всей
машине, включая переданные пакетчиком стенды `~/.prifly-stands/aif-classic*`,
которые обязаны оставаться сравнимыми между выпусками, и авторитеты чужого
проекта. Подтверждение там необратимо и стоило бы чужой работы.

## Непроверенные границы

- Новые HTTP/runtime tests удаляют только данные во временных test fixtures. Реальные пользовательские хранилища не очищались.
- Наблюдение нажатием сделано на одном источнике с семью прогонами. Длинный список с постраничной навигацией подтверждался отдельно (2026-09-08, 385 → 353 Run); одновременно то и другое в одном прогоне не наблюдалось.
- Полный release gate / `make check`, нагрузка на очень больших хранилищах, смешанная работа со старыми бинарниками и уникальное физическое потребление APFS clones/snapshots не квалифицированы.
- Блокировка действует между обновлёнными клиентами. Перед использованием очистки следует обновить все клиенты и закрыть старые процессы. Открытый Engine/driver удерживает хранилище; очистка отказывает и допускает повтор после его закрытия.
- Сканирование диска фоновое; показанный итог может быть неполным при ошибках и не является точным обещанием освобождённых физических блоков. SQLite VACUUM требует дополнительного места. Audit/receipts, настройки, пакеты, независимые imports и используемые данные сохраняются.
