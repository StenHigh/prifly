## 1. Вставка объявляет один вход

- [x] 1.1 `extend.yaml` принимает `input_bindings: {PORT: $stages.STAGE.PORT}`,
      ровно одну запись, источником только выход стадии.
- [x] 1.2 Привязка записывается в стадию вместо пустой карты; имя стадии
      проверяется существующим `project_extension_unknown_stage`.
- [x] 1.3 Отказ сужен до двух случаев: связанный порт, которого шаг не
      объявляет, и второй обязательный вход.

## 2. Проверка остаётся одна

- [x] 2.1 Компиляция отвергает привязку к несуществующему выходу и к стадии, не
      исполняющейся до потребителя. Проверка: оба отказа воспроизведены
      сквозным тестом, своя проверка не заводилась.

## 3. Схема авторинга

- [x] 3.1 `schemas/authoring/extension-v1.schema.json` и встроенная копия учат
      поле; копии совпадают байт в байт. Проверка: справочный документ проходит
      схему, форма без `$stages.` отвергается.

## 4. Завершение

- [x] 4.1 Синхронизировать дельту с основными спеками; `openspec validate --all
      --strict --no-interactive` зелёный.
- [x] 4.2 Evidence: `make check` (48 бандлов) и `make e2e` (169 и 75 команд,
      10 кейсов) зелёные на `4ee5a26`; `verify` 34173844354 и `race` 34173844475
      зелёные. Выпущено в 0.12.7.
