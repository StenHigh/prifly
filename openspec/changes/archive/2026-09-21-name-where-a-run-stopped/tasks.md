## 1. Контракт

- [x] 1.1 `core-next/36` в `internal/runtime/model.go`; строки лестницы в
      `versions.go`, которые сегодня отвечают `core-next/33`, `/34` и `/35`,
      переводятся на него — состояние и read не минтятся.
- [x] 1.2 Capability `run_finish_named` в `compatibility.go` с записанной
      причиной, почему версия состояния не нужна.
- [x] 1.3 Бандл `core-run-finish:36` в `cmd/schema-gen/` и в списке, который
      `PublicSchema` обходит — без записи туда бандл собирается, но ни один
      reader его не находит. `make schemas-check` зелёный. Бандлы 34 и 35
      изменились не от этой правки, а от соседней, которая чинит их имя.

## 2. Ответ

- [x] 2.1 `RunFinish` и `NextView.Finish`; заполняется только для терминального
      завершённого Run.
- [x] 2.2 Ребро выводится через `flow.Plan.Next` — ту же функцию, которой шёл
      драйвер; неоднозначность даёт отсутствие обеих частей.
- [x] 2.3 Тесты: заход, завершённый ребром в finish, называет
      `from`/`verdict`/`to`/`outcome`; заход с двумя исполненными стадиями,
      ведущими в ту же finish, называет место без ребра. Каждое свойство
      вырезано по отдельности и даёт красный.

## 3. Читателю

- [x] 3.1 `examples/troubleshooting.md`: «Где заход свернул к своему исходу».
- [x] 3.2 `make ci-check` зелёный: fmt-check 307 файлов, refusal-check 165 файлов;
      `make e2e` зелёный.
